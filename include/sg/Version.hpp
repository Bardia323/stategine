// Stategine - the version, in one place. CMake reads it from here, so the
// header a project compiles against and the tag it fetched cannot disagree.
#pragma once

#define SG_VERSION_MAJOR 0
#define SG_VERSION_MINOR 1
#define SG_VERSION_PATCH 0
#define SG_VERSION_STRING "0.1.0"
